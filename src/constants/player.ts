/**
 * Player model dimensions (proportional to field scale)
 */

// Body dimensions
export const BODY_HEIGHT = 1.2;
export const BODY_RADIUS = 0.35;

// Head dimensions
export const HEAD_RADIUS = 0.25;

// Leg dimensions
export const LEG_HEIGHT = 0.6;
export const LEG_RADIUS = 0.12;

// Arm dimensions
export const ARM_LENGTH = 0.5;
export const ARM_RADIUS = 0.08;

// Disc dimensions (standard ultimate disc is ~27cm diameter)
export const DISC_RADIUS = 0.14;
export const DISC_HEIGHT = 0.025;
